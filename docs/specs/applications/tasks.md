# Formal applications — tasks

Status: BASELINED

The current implementation predates formal SDD. Existing behavior is treated as
baseline subject to the gap report.

- [x] Extract established intended behavior into `APP` requirements.
- [x] Record current technical design.
- [ ] Resolve any `APP` findings marked `SPEC GAP` or `CODE GAP` in
      `docs/spec-code-gap-report.md`.
- [ ] For the next behavioral change, add requirement IDs before implementation.
- [ ] Update traceability after each implementation change.
- [ ] Run `make format` and `make check`.

## CHANGE: create selected draft applications during finalization

- [x] `APP-002` Create selected discussed programs as initial DRAFT Applications during Lead finalization.
- [x] `APP-003` Require an active concrete offering for Program-level selections.
- [x] `APP-006` Link every converted LeadProgramInterest to its created Application.
